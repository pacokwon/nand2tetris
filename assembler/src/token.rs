#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Token {
    Plus,
    Minus,
    Equal,
    LParen,
    RParen,
    Semicolon,
    Bang,
    Ampersand,
    At,
    Pipe,
    Symbol(String),
    RegA,
    RegM,
    RegD,
    RegAM,
    RegAD,
    RegMD,
    RegAMD,
    JGT,
    JEQ,
    JGE,
    JLT,
    JNE,
    JLE,
    JMP,
    Number(u16),
    Newline,
    Eof,
}
